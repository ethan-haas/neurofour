"""The NeuroGolf benchmark: positions, strength, ladder, cost, score.

(Named `neurogolf` rather than `bench` for tooling-compatibility; it implements
the benchmark described in METRIC.md / ARCHITECTURE.md as `app/bench`.)
"""
